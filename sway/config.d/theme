# Apply gtk theming
exec_always ~/.config/sway/scripts/import-gsettings

# Set inner/outer gaps
gaps inner 3
gaps outer 3

# Hide titlebar on windows: 
default_border pixel 2

# Default Font
font pango:JetBrainsMono 10

# Thin borders:
smart_borders off

# Set wallpaper:
exec ~/.azotebg

# Title format for windows
for_window [shell="xdg_shell"] title_format "%title (%app_id)"
for_window [shell="x_wayland"] title_format "%class - %title"

## Window decoration
# class                 border  backgr. text    indicator child_border
client.focused          #fdbc4b #434c5e #81a1c1 #fdbc4b   #fdbc4b
client.focused_inactive #fdbc4b #2e3440 #d8dee9 #fdbc4b   #fdbc4b
client.unfocused        #fdbc4b #2e3440 #d8dee9 #fdbc4b   #fdbc4b
client.urgent           #fdbc4b #ebcb8b #2e3440 #fdbc4b   #4c566a

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
bar {
    swaybar_command waybar
}
